<%#フィード用パーシャル%>
<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items, params: {controller: "static_pages", action: "home"} %>  <%#エラーを回避するためにコントローラとアクションを指定%>
<% end %>